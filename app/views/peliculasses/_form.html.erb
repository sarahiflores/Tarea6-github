<%= form_for(@peliculass) do |f| %>
  <% if @peliculass.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@peliculass.errors.count, "error") %> prohibited this peliculass from being saved:</h2>

      <ul>
      <% @peliculass.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :NombredePeliculas %><br>
    <%= f.text_area :NombredePeliculas %>
  </div>
  <div class="field">
    <%= f.label :rate %><br>
    <%= f.number_field :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
